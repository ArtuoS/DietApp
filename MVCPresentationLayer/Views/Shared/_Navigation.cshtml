<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <img alt="image" class="rounded-circle" src="~/Images/profile_small.png" />
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold">@User.Identity.Name</span>
                        <span class="text-muted text-xs block">Perfil <b class="caret"></b></span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        <li><a class="dropdown-item" href="@Url.Action("Home", "Index")">Sair</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    <img alt="image" src="~/Images/logo_Icon_small.png" />
                </div>
            </li>
            <li class="@Html.IsSelected(controller: "Home", action: "Index")">
                <a href="@Url.Action("Index", "Home")"><i class="fa fa-user"></i> <span class="nav-label"></span>Perfil<span class="label label-primary float-right">Início</span></a>
            </li>

            @{if (User.FindFirst("Role").Value == "Admin")
                {
                    <li class="@Html.IsSelected(controller: "Meal", action: "Insert")">
                        <a href="@Url.Action("Insert", "Meal")"><i class="fa fa-cutlery"></i> <span class="nav-label"></span>Refeições<span class="label label-danger float-right">Admin</span></a>
                    </li>
                    <li class="@Html.IsSelected(controller: "User", action: "Users")">
                        <a href="@Url.Action("Users", "User")"><i class="fa fa-users"></i> <span class="nav-label"></span>Usuários<span class="label label-danger float-right">Admin</span></a>
                    </li>
                }
            }
        </ul>
    </div>
</nav>